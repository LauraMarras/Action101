import numpy as np
from scipy.stats import rv_continuous, pareto


def pareto_right(null_distro, critical_value, critical_p, tail_percentile=0.9):

    permutations = null_distro.shape[0]






















    return pvalue, critical_value_at_p
